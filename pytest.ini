[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short --strict-markers
markers =
    integration: tests that exercise the full Spark + Delta stack
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
